# SonarQube

When running the project it is nessausary to have the right dependencies. So before any adjsutments can be made or any code can be run, the following command needs to be run to add the required dependencies.

```
npm install --save-dev sonarqube-scanner
```

---

## SonarCloud